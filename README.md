# DMS – Dorm Management System (Android App)

Android-приложение для управления общежитием (Dorm Management System, DMS).  
Позволяет студентам и сотрудникам просматривать новости, документы, информацию о проживании, записываться на спортивные секции и управлять своим профилем.

Бэкенд-часть (API) находится в отдельном проекте и подключается через `Retrofit`.

---

## 🔧 Tech Stack

- **Language:** Kotlin
- **Architecture:** Activity + Fragments
- **Networking:** Retrofit + OkHttp
- **UI:** XML layouts, RecyclerView, Navigation Drawer
- **Data:** SharedPreferences (через `SessionManager`) для хранения токена и данных пользователя
- **Build System:** Gradle (KTS)

---

## 📱 Основные возможности

- **Авторизация**
  - Экран логина (`LoginActivity`)
  - Отправка логин-данных на API
  - Сохранение токена и информации о пользователе в `SessionManager`
  - Авто-редирект на `MainActivity` при наличии валидной сессии

- **Главный экран / Навигация**
  - `MainActivity` с Navigation Drawer / Bottom Navigation
  - Быстрый доступ к разделам:
    - Новости
    - Документы
    - Общежитие (Residence)
    - Спорт (Sports)
    - Профиль

- **Новости (News)**
  - Список новостей (`NewsFragment`) — отображение через `RecyclerView` и `NewsAdapter`
  - Детальный просмотр новости (`NewsDetailActivity`)
  - Добавление / редактирование новости (`AddEditNewsActivity`)  
    (обычно доступно для администратора/персонала)

- **Документы (Documents)**
  - Список документов (`DocumentsFragment`) с помощью `DocumentsAdapter`
  - Просмотр основных атрибутов документа
  - Возможность расширить до скачивания/открытия PDF по ссылке

- **Общежитие (Residence)**
  - Экран `ResidenceFragment`
  - Информация по проживанию, комнатам, правилам, заявкам и т.д. (контент подтягивается с API)

- **Спорт / Секции**
  - Экран `SportsRegistrationFragment`
  - Просмотр доступных активностей / секций
  - Возможность зарегистрироваться (через API-запрос)

- **Профиль пользователя**
  - Экран `ProfileFragment`
  - Отображение данных пользователя (`UserProfile`)
  - Кнопка выхода из аккаунта (очистка сессии и переход на `LoginActivity`)

